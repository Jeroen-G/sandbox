import React from 'react';
import { SafeAreaView } from 'react-native';

import { Morphing } from '@/components/Buttons/Morphing';

export default function ButtonsScreen() {
    return (
        <SafeAreaView>
            <Morphing />
        </SafeAreaView>
    );
}
